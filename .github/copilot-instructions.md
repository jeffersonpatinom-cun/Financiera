# ETL Script for FCT_Ventas

This script will extract data from the SQL query used in Power BI's FCT_Ventas table and load it into a SQL Server table for ETL automation.

- Edit the connection parameters as needed.
- The SQL query will be filled in from the Power BI model.
- The script uses pandas and sqlalchemy for ETL.
